# varequal 0.0.2

* Added a `NEWS.md` file to track changes to the package.
* Bugfixed: `check_var_equal()` handles method `MBF` & `BF` incorrectly.
* Modified: rearrange arguments order of each explicit functions.
* Added vignettes
